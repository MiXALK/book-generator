# Book Generator

Book Generator is a project for building software that helps generate books.
The implementation stack and runtime commands will be documented here as they
are introduced.

## Project Documentation

- `AGENTS.md` describes how AI coding agents should work in this repository.
- `CONVENTIONS.md` is the authoritative source for project conventions,
  structure, constraints, and design decisions.

## Development

No application runtime, package manager, or test command has been selected yet.
When tooling is added, document the canonical commands in this section:

- Install dependencies
- Run the application
- Run tests
- Run linting or formatting
- Build release artifacts

## Repository Structure

```text
.
├── AGENTS.md
├── CONVENTIONS.md
├── README.md
└── .gitignore
```

Update this structure when source code, tests, documentation, or tooling are
added.

## Contributing

Keep changes small, intentional, and aligned with `CONVENTIONS.md`. If a change
introduces or modifies a project convention, update `CONVENTIONS.md` in the same
change.
